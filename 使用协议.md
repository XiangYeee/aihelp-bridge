# AIHelp Stack 使用协议

版本: 0.2.7  
仓库: https://github.com/XiangYeee/aihelp-stack  
许可: LicenseRef-AIHelp-Internal（见 LICENSE）  
生效: 随本仓库公开提交

本协议用于商店上架、GitHub 导入与同事安装。安装即表示接受 LICENSE 与本协议。

## 1. 授权对象与范围

授权对象: AIHelp 员工。  
使用场所: 公司内网，或已连接公司 VPN 的设备。  
授权行为: 安装插件、加载技能、使用 MCP 客户端配置访问公司开发平台。

未授权: 向非员工再分发、作为公网通用插件使用、把本插件当 VPN 或 MCP 服务端。

本插件只包含技能与 MCP 客户端配置，不包含 VPN 客户端，不包含 MCP 服务端进程。

## 2. 使用前提

必须能访问公司内网 MCP 网关。公网不可用。  
凭据只放本机，不进 git，不写进商店描述。  
本机已有同名用户 MCP 时，用户配置优先于插件。

## 3. 配置

Grok / Codex / Claude：将下列 5 项写入本机环境变量。

Gemini：安装时填写 5 项 settings，不会读取本机环境变量。

Cursor：不要写本机环境变量。安装完成后立刻在插件 Configure 里填写同名 5 项。

值不要带 `Basic` / `Bearer` / `AccessKey` 前缀。

AIHELP_MCP_HOST: MCP 网关主机名或 IP。不要 `http://`，不要端口。向同事或内部文档索取。  
AIHELP_MCP_BASIC: account / wiki / jenkins / zentao / elk 的 HTTP Basic。生成: `echo -n 'user:password' | base64`，只填 Base64。  
AIHELP_GITLAB_MCP_TOKEN: 公司 GitLab → Preferences → Access Tokens，权限含 `api`。只填 token。  
JUMPSERVER_MCP_ACCESSKEY: JumpServer 个人中心 API Key。填 `id:secret`。  
AIHELP_LANGFUSE_MCP_BASIC: Langfuse MCP 的 Base64(user:password)。生成方式同 BASIC。

## 4. 安装与导入

Grok Build: `grok plugin marketplace add XiangYeee/aihelp-stack`，再 `grok plugin install aihelp-stack --trust`。  
Claude Code: `claude plugin marketplace add XiangYeee/aihelp-stack`。  
Codex: `codex plugin marketplace add XiangYeee/aihelp-stack`。  
Gemini CLI: `gemini extensions install https://github.com/XiangYeee/aihelp-stack`。

Cursor 从 GitHub 导入（团队 Marketplace）:

1. 打开 Cursor Dashboard → Plugins。
2. Team Marketplaces → Add Marketplace / Import from Repo。
3. 仓库地址填 `https://github.com/XiangYeee/aihelp-stack`。
4. Cursor 读取 `.cursor-plugin/marketplace.json`，应看到插件 `aihelp-stack`。
5. 安装完成后立刻打开插件 Configure，填写第 3 节 5 项。不要配本机环境变量。

Cursor 个人安装: 在插件搜索中粘贴同一 GitHub 地址。导入后必须马上 Configure，否则 MCP 连不上。

## 5. 能力与边界

包含技能: `aihelp-guide`、`aihelp-bug`、`aihelp-ship`。  
包含 MCP 客户端: GitLab、Jenkins、ELK、DB、Apollo、账号、Wiki、禅道、JumpServer、Langfuse、Zabbix。  
不包含: `aihelp-commit`、后台登录辅助、本机服务拉起、VPN、MCP 服务端。

上线通知模板只用占位符，不用真实禅道单号和 MR 链接。  
插件不保存、不回传、不测试客户 webhook / 邮箱 / 手机号等敏感数据。

## 6. 安全

Grok 等端密钥放本机环境变量；Cursor 密钥只放插件 Configure。  
禁止把真实 token、密码、内网 IP 写进仓库或商店描述。  
官方商店未通过审核时，同事仍可用本 GitHub 仓库作为 marketplace 源。

## 7. 免责

软件按现状提供。因内网中断、凭据错误、MCP 握手失败、错误发版或数据损失产生的后果，由使用人自行承担。

## 8. 上架说明

本仓库公开，供商店审核与 GitHub 导入。公开仓库不扩大第 1 节授权。  
许可不是 OSI 开源许可。若某商店强制要求 MIT/Apache，该商店可能拒审；GitHub 导入不受此限制。
